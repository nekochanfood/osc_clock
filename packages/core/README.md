# core

Rust core library + CLI that sends date and time to VRChat via OSC.

## Build

From the repository root:

```bash
cargo build -p osc_clock
```

Run the CLI:

```bash
cargo run -p osc_clock -- --help
```